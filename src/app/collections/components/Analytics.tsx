import { fetchOwnersData } from '../../services/fetchOwnersData'

const Analytics = () => {
  const ownersList = await updateFloorPrice()
  return (
      <div className="min-h-10 w-full flex items-center pb-8 px-4 lg:px-8 xl:px-16">
        
      </div>
  )
}

export default Analytics
