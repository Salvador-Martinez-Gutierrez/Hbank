/*
import DashboardButton from './DashboardButton'
import DefiAnalyticsButton from './DefiAnalyticsButton'
import FungibleButton from './FungibleButton'
import NftAnalyticsButton from './NftAnalyticsButton'

interface SideNavProps {
  accountId: string
}

const SideNav = ({ accountId }: SideNavProps) => {
  return (
    <nav className='flex flex-col sticky left-0 top-[75px] w-full md:w-48 items-left bg-neutral-900 text-neutral-200'>
      <div className='flex flex-col gap-2 px-4 lg:px-8 xl:px-16'>
        <h2 className='text-xl font-bold pt-9'>
          {accountId}
        </h2>
        <DashboardButton accountId={accountId}/>
        <FungibleButton accountId={accountId}/>
        <NftAnalyticsButton accountId={accountId}/>
        <DefiAnalyticsButton accountId={accountId}/>
      </div>
    </nav>
  )
}

export default SideNav
*/
